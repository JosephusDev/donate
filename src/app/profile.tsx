import { s } from '@/styles/app/menus'
import { Text, View } from 'react-native'

export default function Profile() {
	return (
		<View style={s.container}>
			<Text style={s.text}>Brevemente ...</Text>
		</View>
	)
}
